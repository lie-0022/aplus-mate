# A+ Mate 시간표 도입 설계 (다음 학기 실데이터 적용 대비)

> 2026-06-15 분석. 멀티에이전트 분석 + pdfplumber PoC로 실현성·설계를 확정해 둔 청사진.

---

## ★ 2026-07-09 방향 확정 (사용자) — "계획부터 천천히"

이전엔 "다음 학기 시간표 나오면 착수"였지만, **방향 변경: 지금 있는 2026-1(이번/지난 학기)부터 세팅**하고 다음 학기로 이어간다. 순서:

1. **2026-1 실데이터 세팅** — 다음 학기를 기다리지 않고 지금 학기 시간표부터 적재.
2. **과별로 나누기** — 수업을 학과(단과/학부/과) 단위로 분류·필터. (교무처 데이터에 학부 컬럼 있음 — PoC 행에 `사회복지학부` 존재.)
3. **교양/전공 구분** — 각 수업을 교양(교필·교선) / 전공(전필·전선)으로 태깅.
4. **다음 학기 연속성** — 2026-2 시간표가 들어오면 **같은 수업(과목코드 동일)은 "같은 수업"으로 유지**(중복 생성 X). 리뷰·수업 정체성은 학기 넘어 누적, 개설(분반·시간표)만 학기별로 새로.

**✅ P0 데이터 소스 확정 (2026-07-09 검증):**
- **소스 = 백석대 수강편람 학과별 PDF**. 목록 페이지 `https://www.bu.ac.kr/sites/web/intro/2026_1.html`.
  - URL 패턴: `https://www.bu.ac.kr/sites/web/files/sugang/2026_1/2026_1_{코드}.pdf`
  - **18개 학과 코드**: 기독교학부(chr)·어문학부(lan)·사회복지학부(wel)·경찰학부(law)·경상학부(biz)·관광학부(tou)·외식산업학부(foo)·사범학부(edu)·컴퓨터공학부(com)·첨단IT학부(sma)·보건학부(hea)·간호학과(nur)·디자인영상학부(des)·스포츠과학부(spo)·문화예술학부(art)·자유전공학부(dls)·국제학부(bug)·혁신교육플랫폼대학(iis). → **COHORT_DEPARTMENTS와 1:1** (프로필 학과 = 시간표 과 = 정확일치 연결).
- **파싱 실측(컴퓨터공학부 com.pdf, 220KB·16p·%PDF-1.6 디지털)**: `pdfplumber.extract_tables(vertical_strategy='lines', horizontal_strategy='lines')`로 **15컬럼 클린 분리 확인**. 강의표는 5페이지부터(1~4p는 표지·안내). 기본 전략(선 없음)은 0표 → **line 전략 필수**.
- **전공 PDF 컬럼(교양과 다름!)**: `트랙 · 구분(기초교과군/핵심교과군…) · 과목코드 · 과목명 · 시수 · 학점 · 제한인원 · 신입생 · [1학기: 담당교수·요일/교시·강의실·특이사항·핵심역량(비율)] · [2학기: 담당교수·요일/교시…]`. 즉 **1·2학기 동시 표기**(개설 예정), 요일교시 `목4,5/ 사`(교시 리스트 + 사이버), 핵심역량 `전공(100)`·`전공(50),혁신(50)`. → 파서는 **교양/전공 포맷별 컬럼 매핑** 필요(PoC 교양은 이수구분·영역 중심 15컬럼, 전공은 트랙·구분 중심 15컬럼).

**✅ 범위 확정(2026-07-09 사용자): 18학과 전부 + 교양 포함.**

### ★★ 후기 연속성 아키텍처 (이 기능의 심장 — 2026-07-09)
**요구**: 방학에 2026-1 시간표 적재 → 학생이 지난 학기 수업 후기 작성 → 2026-2 시간표 나오면 **같은 수업엔 후기가 이어져야** 함.

**핵심 원칙**: **후기는 "개설(학기·분반)"이 아니라 "과목(subject)"에 붙는다.** 과목마다 학기·분반이 달라도 동일한 식별자 `courseGroupId`를 부여하고, 후기 집계·표시를 courseGroupId 단위로 한다. → 학기가 바뀌어도 같은 과목 = 같은 courseGroupId = **후기 자동 승계**.

**두 층위 분리:**
- **개설(offering) = `courses` row**: 학기(semester)·분반·요일교시·교수·강의실·정원·교양전공·과목코드 + **courseGroupId**. 수강등록·매칭·팀·게시판·설문은 여기(courseId)에 붙는다(학기별 활동).
- **과목(subject) = courseGroupId 그룹**: 후기가 집계되는 단위. 별도 테이블 없이 courses.courseGroupId로 묶는다(경량안). 필요시 `subjects` 테이블로 승격 가능(정석안).

**후기 저장/표시(경량안, 권장):**
- 후기는 지금처럼 개설(courseId)에 저장하되, **summary/list 집계를 `courseGroupId`로** 한다 → `getCourseReviewSummary(courseGroupId)` = 그 courseGroupId를 가진 모든 courses의 후기 합산. 2026-2 개설을 봐도 2026-1 후기가 함께 뜬다.
- 후기 작성: 학생이 자기가 들은 **2026-1 개설을 검색→참여(enroll, semester=2026-1)** 후 작성(현행 isUserEnrolled는 학기 무관이라 그대로 동작).

**✅ courseGroupId 키 확정 (2026-07-09 PDF 실측):** `courseGroupId = 과목코드 앞 5자리`.
- **코드 구조 = 5자리 과목번호 + 2자리 분반번호** (7자리 균일). 컴·첨단IT·경상·간호·문화예술·교양 전부 동일. 증거: C언어 `0367001~0367010`(분반 01~10, 과목 `03670`), 이산수학 `0154701/02`(과목 `01547`), 교양 반려동물과현대사회 `3168801~03`(과목 `31688`).
- **앞 5자리 → 과목명 유일 ≈ 100%** (com 56/56·sma 62/62·biz 70/70·nur 49/49·교양 15/15). 몇 건 예외(art 144/146: 전공실기·크로스리스트)는 엣지로 별도 처리.
- **코드는 학과 전역** — C언어(`0367001`)·파이썬(`0948101`)·자료구조론(`0218601`)이 컴퓨터공학부·첨단IT학부에 **동일 코드**. → 같은 과목 = 같은 코드(university-wide). **학과를 키에 안 붙여도 됨.**
- 분반(같은 과목 여러 반)은 앞5자리 동일 → 후기에서 자동 합쳐짐(과목 단위). 교수·시간 차이는 후기에서 구분 안 함(요구가 "같은 수업 = 이어짐").
- **원본(과목코드 전체·과목명·학과) 다 저장** + courseGroupId=앞5자리 파생. 엣지(전공실기 등 코드 불규칙)는 fallback으로 `정규화(과목명)+학과`.
- ⏳ **남은 검증 = 크로스학기 안정성**: `01547`이 2025-2에도 이산수학인가? 과목코드는 교과과정 식별자라 학기 넘어 안정적일 가능성 높음. **2025-2 PDF 1개로 확인 필요**(사용자 제공 예정). 안 맞는 과목만 재매핑.

**포맷(파서 매핑):** 전공 = `트랙·구분·과목코드·과목명·시수·학점·제한인원·신입생·[1·2학기 각각 교수·요일교시·강의실·특이사항·핵심역량]`(15컬럼, 1·2학기 동시표기). 교양 = `구분·과목명·과목코드·학점·교수·요일교시·비고`(단순, 변형 있음). → 헤더로 포맷 감지 후 컬럼 매핑.

**엣지(2026-2 적재 시 처리):** 과목명 소폭 변경(자료구조→자료구조와알고리즘)·코드 개정·과목 분할/통합 → 키 불일치로 후기 단절/오합. 원본 보존 + 수동 재매핑 도구로 대응.

### 갱신된 로드맵 (연속성 반영)
- **P1 스키마**: courses에 semester·courseGroupId·과목코드·category(교양/전공)·subType·교수·정원·hours·campus·competencies 추가, course_schedules(요일×교시), 유니크 교체, create 운영자 제한. **후기 집계를 courseGroupId로 전환.**
- **P2 파싱·시딩(2026-1)**: 18학과 전공 + 교양 PDF, 포맷별 컬럼 매핑, courseGroupId 파생, 멱등 upsert(sourceKey=과목코드+학기), 구글시트 검수.
- **P3 후기 연속성 UI**: 과목 단위 후기 표시(모든 학기 합산), 지난학기 수업 참여→후기, 시간표 격자·과별/교양전공 필터.
- **P4 2026-2 적재**: 같은 과목 courseGroupId 일치 검증 → 후기 승계 확인, 엣지 재매핑, 공강 매칭.

> **다음 실행 후보**: courseGroupId 키 확정을 위해 **여러 학과+교양 PDF의 과목코드 구조 분석**(과목/분반 분리 규칙, 코드 안정성). 이게 연속성 성패라 P1 착수 전에 하는 게 안전.

### 데이터 모델 — 이번 방향 반영(아래 기존 설계에 덧붙임)
- `courses.department`(varchar) — 학과/학부. 전공은 소속 학과, 교양은 `공통`(또는 null). **프로필 학과와 표기 통일**.
- `courses.category`(mysqlEnum `['교양','전공']`) + `courses.subType`(mysqlEnum `['교필','교선','전필','전선','기타']`). (기존 설계의 `courseType`을 이 2단으로 정리.)
- **수업 연속성 키**: `courseGroupId`(과목코드 기반, 학기 무관) = "같은 수업" 식별자. 2026-2 적재 시 courseGroupId 일치 → 기존 그룹에 새 학기 개설을 붙임. **리뷰는 courseGroupId(또는 courseCode) 단위로 집계**해 학기 넘어 쌓이게. (현재 리뷰는 courseId 단위 → 연속성 도입 시 group 단위 집계로 승격 검토.)
- 나머지(유니크 교체·course_schedules·검색필터·공강매칭 등)는 아래 기존 설계 그대로.

### 이번 방향 로드맵 (기존 로드맵 위에)
- **P0. 2026-1 데이터 소스 확보** ← 지금 여기. (엑셀 우선, 없으면 PDF)
- **P1. 스키마 선수정** — semester(courses에)·department·category·subType·courseGroupId·course_schedules 추가, courseCode 유니크, create 운영자 제한.
- **P2. 2026-1 파싱·시딩** — 멱등 upsert, 과별·교양전공 태깅, 사람 검수.
- **P3. 화면** — 과별/교양전공 필터 + 내 시간표 격자 뷰.
- **P4. 2026-2 들어오면** — courseGroupId로 연속성 유지 적재 + 공강 매칭.

> **지금은 P0(데이터 소스·학과목록 확정)만 사용자와 정하면 됨. 코드 착수는 그 다음.**

---


## 확정된 전제(사용자 결정)
- **데이터 소스**: 미정 — 학교 교무처 엑셀/CSV 입수 가능 여부 확인 필요. 받으면 파싱 대부분 회피. 안 되면 PDF 파싱(아래). → 파서는 **PDF/엑셀 양쪽 대응**으로 설계.
- **매칭 단위**: **타입별 차등** — 팀플(project)=분반 고정, 스터디·멘토링=같은 과목 전체. → **`courseGroupId`(과목 그룹키) 필요**.
- **범위**: **교양 + 전공** — 파서는 교양 전용이 아니라 범용. 전공 시간표는 형식이 다를 수 있어 입수 시 재검증.
- **시점**: 실데이터는 다음 학기 시간표 공개 후 적재.

## PoC 결과 — 파싱 실현성 입증 (`scripts/parse_timetable_poc.py`)
- PDF는 **텍스트 레이어 있는 디지털 문서**(스캔본 아님 → OCR 불필요).
- **괘선이 있어 `pdfplumber.extract_tables()`가 15개 컬럼을 자동 분리.** pypdf `extract_text()`의 "붙은 텍스트" 문제가 해소됨 — 거대 정규식 분해 불필요.
- 데이터 행 예: `['교필','기독교인성','교양','사랑의실천','3','0335501','기독교세계관','1','1','40','김호영','목7','진리406','사회복지학부','섬김(100)']` — 시수·학점·인원·교수·요일교시·강의실 전부 별도 컬럼.
- 규모: 교양만 **1,227강의**(교필 883 / 교선 352, 분반까지 별개). → **수동 입력 불가, 자동 파싱 + 검수 필수.**
- 후처리 보정 필요: 권장학년/과목코드 컬럼 경계 미세 어긋남(`'3','0335501'`→과목코드 `30335501`), 2017/2018 이수영역 서브컬럼 병합, 교수 글자사이 공백.

## 데이터 모델 변경안

### courses (수정)
- **유니크 교체**: `(name, professor, university)` → **`(courseCode, university, semester)`** — 현행 제약이 다분반 적재의 1차 장애물(같은 교수·같은 과목명 2분반 = ER_DUP_ENTRY). 과목코드(7~8자리)가 분반 자연키.
- **`semester` 추가**(현재 userCourses에만 있음) — 시간표는 학기별이라 courses에 고정.
- **컬럼 추가**: `campus`, `courseType`(enum 교필/교선), `areaPre2018`, `areaPost2018`, `recommendedYear`(varchar — '전체' 때문), `capacity`(int), `hours`(int 시수), `note`(text 특이사항), `competencies`(json `{name,ratio}[]`), `rawSchedule`(text 원문), `sourceKey`(과목코드+학기, 멱등 upsert 키).
- **`courseGroupId`**(과목명+학교 그룹키) — 타입별 매칭 차등용(스터디·멘토링이 분반 넘어 매칭).
- `courses.create`를 **adminProcedure로 제한**(현재 학생도 생성 가능 — 시간표 도입 후엔 운영자 일괄 적재).

### course_schedules (신규 — 1수업:N교시)
`{ id, courseId(FK), dayOfWeek mysqlEnum['월','화','수','목','금','토','사이버'], period int nullable, room varchar }`
- 연강 `목2,3` → (목,2)·(목,3) 2 row. 사이버혼합 `사/ 화3` → (사이버,null)·(화,3) 2 row.
- 공강 비트마스크·시간충돌·격자 렌더의 데이터 기반.

### 매칭 호환성
분반=courses row로 분리하면 매칭 외래키(courseId)는 **무수정**. team_matches/teams/posts/surveys 전부 그대로. 매칭이 자동으로 분반 단위가 되어 정밀해짐. 타입별 차등은 createMatchRequest/acceptMatch에서 project=courseId, study/mentoring=courseGroupId 기준으로 분기.

## 파싱·시딩 파이프라인 (PDF 경로)
1. (우선) 교무처 엑셀/CSV 입수 시도 → 받으면 컬럼 매핑만.
2. `pdfplumber.extract_tables(vertical_strategy='lines')` 좌표 추출 → 15컬럼 행.
3. 셀 단위 후처리: 과목코드 경계 보정, 시수·학점·인원 분해(이미 분리됨), 요일교시 파싱(연강 리스트·사이버 분리), 교수 공백 제거(복수교수 콤마 보존), 이수영역 서브컬럼 병합.
4. 자동 불변식 검증(코드 7~8자리·시수학점 1~6·인원 1~300·역량비율 합100·이수구분 enum·필드수). 실패행만 LLM 폴백(추측 금지·불명확 null).
5. JSON + `_raw` + 검증 리포트 산출 → **구글시트로 사람 검수**(1코호트엔 Admin 적재 UI 안 만듦).
6. `sourceKey` 기준 **멱등 upsert** 일회성 시딩 스크립트로 적재.
7. 표본 50~100행 원본 수기 대조 95%+ 확인.

## 시간표로 가능해지는 신기능 (놓쳤던 것, 효용순)
| 기능 | 가치 | 난이도 |
|---|---|---|
| **공강 매칭** | 팀원 공통 빈 시간(주간 점유 비트마스크 AND-NOT)으로 후보 정렬 — 팀플 최대 페인 해결, 차별점 | 중 |
| **시간 충돌 경고** | 수강등록 시 내 시간표와 겹치면 경고(막지 않음) | 쉬움 |
| **내 시간표 격자 뷰** | 요일×교시 격자 + 공유 — 에브리타임식 후킹·바이럴 | 중 |
| **검색 필터** | semester(기본)·campus·courseType·credits·dayOfWeek — 분반 폭증 대응 필수 | 중 |
| **매칭 범위 확장** | courseGroupId로 같은 과목 다른 분반 통합(study/mentoring) | 어려움 |
| 졸업요건/이수영역 집계 | 역량기반 교양 학교 차별점·제휴 포인트 | 어려움 |

## 로드맵 (다음 학기 실데이터 확보 후)
0. **데이터 소스 확정** — 교무처 엑셀 가능 여부 + 실제 PDF로 형식 재검증(전공 포함).
1. **스키마 선수정** — 유니크 교체, courses 컬럼·course_schedules·courseGroupId 추가, create 운영자 제한. (schema→db→routers→pages 순)
2. **파싱+시딩** — 위 파이프라인, 구글시트 검수, 멱등 upsert.
3. **검색 필터 + 내 시간표 격자 뷰** — 분반 폭증 대응 + 리텐션.
4. **공강 매칭** — getCourseStudents 반환에 freeOverlap 점수.
5. (2코호트) 매칭 범위 확장 + Admin 적재 탭 + 졸업요건 MVP.

## 남은 미결 (구현 착수 시 확인)
- 캠퍼스: 백석대 다캠퍼스 여부(강의실 건물명만으론 캠퍼스 식별 불가) → campus 컬럼 채울 데이터.
- 핵심역량: 단순 표시만(json 충분) vs 검색·필터·졸업요건(별도 테이블 승격).
- 전공 시간표 형식: 교양과 레이아웃 동일한지 입수 시 PoC 재검증.
- 기존 courseCode null 수동 등록 수업의 fallback 유니크 처리.
- 엣지케이스 추가 확인: 격주·야간·외국인 전용 분반 표기 등(첫 페이지 PoC에서).
